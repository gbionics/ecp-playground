[workspace]
authors = ["siddharth deore <siddharth.deore@gbionics.ai>"]
channels = ["conda-forge"]
name = "ecp-playground"
platforms = ["linux-64"]
version = "0.1.0"
preview = ["pixi-build"]



[tasks]
configure = { cmd = [
    "cmake",
    "-G", "Ninja",
    "-DCMAKE_BUILD_TYPE=Release",
    "-DCMAKE_INSTALL_PREFIX=$CMAKE_INSTALL_PREFIX",
    "-DEP_ENABLE_YARP=OFF",
    # The source is in the root directory
    "-S",
    ".",
    # We wanna build in the build directory
    "-B",
    "build",
]}
configure-test = { cmd = [
    "cmake",
    "-G", "Ninja",
    "-DCMAKE_BUILD_TYPE=Release",
    "-DCMAKE_INSTALL_PREFIX=$CMAKE_INSTALL_PREFIX",
    "-DBUILD_TESTING=ON",
    "-DEP_ENABLE_YARP=OFF",
    # The source is in the root directory
    "-S",
    ".",
    # We wanna build in the build directory
    "-B",
    "build",
]}
build = { cmd = "cmake --build build --config Release", depends-on = ["configure"] }
capabilities = { cmd = ["./scripts/apply-rt-capabilities.sh", "build/actuator-test-spline"], depends-on = ["build"] }
test-spline = { cmd = ["./build/actuator-test-spline"], depends-on = ["capabilities"] }
install = { cmd = ["cmake", "--install", "build", "--config", "Release"], depends-on = ["build"] }
test = { cmd = "cmake --build build --config Release && ctest --build-config Release --output-on-failure --test-dir build", depends-on = ["configure-test"] }
format = { cmd = "clang-format -i $(git ls-files '*.cpp' '*.h' '*.cxx' '*.hpp')" }

[dependencies]
clang-format = "*"
tomlplusplus = "*"
soem = "*"
cmake = "*"
ninja = "*"
cxx-compiler = "*"